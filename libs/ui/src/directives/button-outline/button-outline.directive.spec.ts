import { ButtonOutlineDirective } from './button-outline.directive';

describe('ButtonOutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonOutlineDirective();
    expect(directive).toBeTruthy();
  });
});
