const cardComponent = ['card', 'card-title', 'card-body', 'card-actions'];

const cardImage = ['image-full'];

const cardPadding = ['card-normal', 'card-compact'];

const cardDisplay = ['card-side'];

const cardStyle = ['card-bordered'];

export default {
  cardComponent,
  cardImage,
  cardPadding,
  cardDisplay,
  cardStyle
};
